# Ficheros de configuración

Este repositorio contiene mis dotfiles: configuraciones y ajustes para mi entorno de desarrollo y escritorio. 💼✨

Herramientas configuradas y dependencias principales:

- 🖥️ Hyprland — gestor de ventanas (config en `hyprland/`)
	- Dependencias comunes: libinput, wlroots, wl-clipboard
- 🐍 Neovim — editor (config en `nvim/`)
	- Plugins: lazy.nvim, nvim-treesitter, nvim-lspconfig, mason
- 🐧 Kitty — emulador de terminal (config en `kitty/`)
	- Dependencias: fonts (Nerd Fonts recomendadas), libxcb
- 🎛️ Rofi / Wofi — lanzadores (config en `rofi/` y `wofi/`)
- 🖨️ Fonts — directorio `fonts/` con tipografías instaladas (p. ej. Fira Nerd). Más info: https://www.nerdfonts.com/
- 🧰 Herramientas de desarrollo: Lazygit, DAP (Delve para Go), Conform para formateo

Notas:
- Revisa cada carpeta para ver la configuración detallada y comentarios.
- Algunas dependencias dependen de tu distribución; instala los paquetes del sistema correspondientes.

